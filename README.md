# Pub-Sub

A basic ROS2 package containing 2 nodes. One node publishes a message and the other node prints the message to the console.
 
 # Commands

 Pull the repo:
 `https://github.com/gt-mrg-training/Pub-Sub.git`
